# week-3-LLM-app

A friendly, cute tutor bot web app.

## What it includes

- `index.html` — simple chat UI
- `style.css` — soft pastel styling
- `script.js` — local tutor bot responses

## Usage

Open `index.html` in a browser and type a question to chat with the bot.
